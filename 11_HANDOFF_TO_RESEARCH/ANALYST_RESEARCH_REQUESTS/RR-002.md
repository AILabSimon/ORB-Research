# RR-002 — Prospective discriminator, and the initial stop behind a pending-order entry

DATE: 2026-09-12 · FROM: Analyst Agent · PRIORITY: HIGH (not blocking) · STATUS: OPEN
CANDIDATE/EXPERIMENT: EXP-001/006 (falsified) → EXP-008 (next construction)

## What the measurement found

Across 3,283 qualifying events on a NAS100 proxy, 2013–2026, the probability that favourable
excursion exceeds adverse excursion from the taught entry is **50.3%–51.9% at every horizon**,
in both directions, in every year, and it replicates on US500. Gross expectancy is flat across
1R/2R/3R targets — the signature of a driftless walk. The failed-breakout reversal is worse: its
source-supported targets sit nearer than its stop, so ≥2R is geometrically unreachable.

Two of the three things Max asserts about the event are therefore not visible in the data: that
the break is directionally informative, and that wide-range mornings are better (CL-012 points
the wrong way — widest quintile is the worst).

## Q1 — Is there a prospective discriminator? (U-01)

This is now the single question that could overturn the result. If Max, or anyone, can say
*before* the outcome whether a given break is the continuation or the reversal, then the
symmetry above is a **representation failure**, not absence of edge, and everything gets retested
under that discriminator. If no such statement exists, the finding stands and F1–F4 are closed.

What is needed, in order of value:
1. Any segment where Max states, **before** price resolves, which of the two he is taking and on
   what observable. Your leads: V8's blind bar-replay (EX-13/EX-14), `0WddcphxAo8`, `NF0qHcXp50o`.
2. Confirmation or refutation of the sequencing rule you inferred from EX-13/EX-14 — *always take
   the break; if it fails, take the reversal*. Is that ever **stated**, or is it only what happened
   once on camera? I can test it mechanically, but only if it is a rule rather than a coincidence.
3. The **free WealthCharts ORB indicator** (V2 33:18) is claimed to "show the actual breakouts
   versus the reversals". If it is inspectable without purchase, its logic would settle U-01
   directly. If it requires payment, stop — do not buy it.

Please report a **negative** finding explicitly if you find no prospective statement. A confirmed
absence is as decision-relevant here as a discovery, and it closes F1–F4 cleanly.

## Q2 — The initial stop behind a pending-order entry (C-01 / R-031)

My next experiment tests what Max **does** rather than what he teaches: a resting order at the
ORB edge instead of a 15-minute body close. That entry has completely different risk geometry —
the taught entry's stop is a median 7.99 points and is below round-trip cost 29% of the time,
whereas an edge entry needs an externally defined stop.

R-031 records an 80/70/60-tick platform **trail** (≈20/17.5/15 NQ points). I need to know:
1. Is that ever used as an **initial** stop, or only as a trail after entry?
2. Is any fixed point/tick initial stop stated anywhere for the pending-order entries in V6/V7?
3. When he "lets a pending order wick him in" before a body close (V6 02:07, V7 07:16), where is
   the protective stop at that moment?

**Why it changes the decision:** if a fixed ~20-point initial stop is source-supported, EXP-008 is
a source-faithful test. If it is not, I must invent the stop, and the experiment becomes an
Analyst construct that can only ever be evidence about the concept, not about Max's method. I
will run it either way; the answer determines how the result may be labelled.

## Not requested
C-03 (holding period) — resolved by measurement: 180 minutes captures 76% of end-of-day
excursion. U-04 (six-or-seven-candle timeframe) — deferred; F5's gate is disjunctive and cannot
discriminate prospectively, so an answer cannot currently change a decision.
